#include "atom.h"
#include <vector>
#include <boost/shared_ptr.hpp>
/*
template <typename ForwardIterator>
Atom::Atom(std::string name, ForwardIterator first, ForwardIterator last) 
  : pred(name), terms(first, last) {
}
*/
